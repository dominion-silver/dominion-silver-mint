# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "pyth-solana-receiver-sdk"
version = "0.6.1"
authors = ["Pyth Data Association"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "SDK for the Pyth Solana Receiver program"
readme = "README.md"
license = "Apache-2.0"
repository = "https://github.com/pyth-network/pyth-crosschain"
resolver = "1"

[lib]
name = "pyth_solana_receiver_sdk"
crate-type = ["lib"]
path = "src/lib.rs"

[dependencies.anchor-lang]
version = ">=0.28.0"

# DOMINION VENDOR PATCH (2026-05-19): upstream pyth 0.6.1 `use borsh::`
# + `#[derive(BorshSerialize/BorshDeserialize)]` with NO `borsh` in its
# manifest. Modern `borsh-derive`'s `proc_macro_crate::crate_name`
# resolution then panics ("Could not find borsh ..."). Pin borsh to the
# 0.10.x line so it (a) is found by the derive, (b) unifies with
# anchor-lang 0.31.1's borsh (also 0.10.x) so `params.try_to_vec()`
# resolves the same trait, (c) matches the committed Cargo.lock (0.10.4).
[dependencies.borsh]
version = "0.10.4"

[dependencies.bytemuck_derive]
version = "<=1.8.1"

[dependencies.hex]
version = ">=0.4.3"

[dependencies.pythnet-sdk]
version = "2.1.0"
features = ["solana-program"]

[dependencies.solana-program]
version = ">=1.16.0"
